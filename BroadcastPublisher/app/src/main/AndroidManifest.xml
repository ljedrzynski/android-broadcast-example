<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.devone.android.broadcastpublisher">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <permission-group android:name="pl.devone.android.PERMISSION_GROUP"
        android:label="PERMISSION_GROUP">
    </permission-group>

    <permission android:name="pl.devone.android.BROADCAST_PERMISSION"
        android:permissionGroup="pl.devone.android.PERMISSION_GROUP"
        android:label="BROADCAST_PERMISSION">
    </permission>

    <uses-permission android:name="pl.devone.android.BROADCAST_PERMISSION">
    </uses-permission>


</manifest>